/*
function weightedRand(spec) {
  var i, sum=0, r=Math.random();
  for (i in spec) {
    sum += spec[i];
    if (r <= sum) return i;
  }
}
var weightedRand({0:0.8, 1:0.1, 2:0.1});
*/
console.log('test')